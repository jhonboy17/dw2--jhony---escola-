# Gestão Escolar – Projeto DW2

Mini sistema escolar com **FastAPI + SQLite** (backend) e **HTML/CSS/JS** (frontend).

## Como rodar

1. Instalar dependências:
   ```bash
   pip install -r backend/requirements.txt
